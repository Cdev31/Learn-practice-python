git init= crear repositorio. (.git)
git status= estatus de los archivos.
git add <file_name>= agregar archivos al rastreo.
git rm --cached <file_name> = para quitar cambios del git add.
git commit -m = agregar cambios al repositorio.
git config --global = configuraciones globales
git config --list = ver configuraciones
git config --list --show-origin = ver donde se encuentra las configuraciones
git log <file_name> = para ver los commits de los archivos
git diff <id commit> = sirve para comparar los cambios de un commit con otro,
el orden es importante.
git checkout <Id_commit> = sirve para cambiar a un commit 
especifico.
git branch <name_branch> = sirve para crear una rama desde 
el ultimo commit.
git merge <name_branch> = sirve para traer cambios de una 
rama a la actual.
git config --global init.defaultBranch main = rama main por 
defecto.

git branch -M main = pasar todo los cambios de branch a main 
git remote add name_origin url = este sirve para agregar un repositorio remoto
git remote -v = sirve para enlistar los repositorios remotos
git pull origin master --allow-unrelated-histories = sirve para traer cambios del repositorio con historia no relacionadas
git push origin master = sirve para mandar datos en tu repositorio remoto
ssh-keygen -t rsa -b 4096 -C kalet.svdev@gmail.com = crea una llave ssh
eval SSH_AUTH_SOCK=/tmp/ssh-5DSDwJamIEwH/agent.49; export SSH_AUTH_SOCK; SSH_AGENT_PID=50; export SSH_AGENT_PID; echo Agent pid 50; = para ver si el servidor ssh esta corriendo
ssh-add /home/kalet/.ssh/id_rsa = para agregar la llave al entorno
git remote set-url url_name or name newUrl = esta forma cambia la url a la que se le hace push
git log --all --graph --decorate --oneline = mejora la forma en como se presentan los logs
